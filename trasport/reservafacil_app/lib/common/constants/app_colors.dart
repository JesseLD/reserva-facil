import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Colors
  static const primary = Color(0xFFFF9701);
  static const primaryAlternative = Color(0xFFFF6900);
  static const primaryLight = Color(0xFFFFC401);
  static const primaryDark = Color(0xFFC66900);

  // Neutral Colors
  static const black = Color(0xFF121214);
  static const gray = Color(0xFF444444);
  static const darkGray = Color(0xFF252527);

  // Accent Colors
  static const warningOrange = Color(0xFFFF9701);
  static const dangerRed = Color(0xFFCE4F51);
  static const successGreen = Color(0xFF4CAF50);
  static const infoBlue = Color(0xFF2196F3);

  // Gradient Colors
  static const primaryGradient = [primaryAlternative, primaryLight];
}
