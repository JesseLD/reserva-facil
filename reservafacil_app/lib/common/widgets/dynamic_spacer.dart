import 'package:flutter/material.dart';

class DynamicSpacer extends StatelessWidget {
  const DynamicSpacer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 48);
  }
}